[pytest]
testpaths = tests
pythonpath = .
python_files = test_*.py
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
markers =
    e2e: End-to-end tests that require a live server on localhost:8000 (deselect with '--ignore=tests/e2e')
